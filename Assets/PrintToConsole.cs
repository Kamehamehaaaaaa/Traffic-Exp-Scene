using UnityEngine;

public class PrintToConsole : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("This is me. Peep Peep!!!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
